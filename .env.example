# Asset Loading Configuration
# Path to assets directory
# - For direct/local use: relative paths like ./src/assets
# - For Docker: use absolute paths (e.g., C:/Users/Username/Desktop/assets)
ASSETS_PATH=../../src/assets

# Asset Server Configuration
WEBSRV_PORT=8081
WEBSRV_PORTSSL=8082
WEBSRV_INTERNAL_PORT=8083
HTTP_USE_SSL=false

# HTTP protocol toggles
# WEBSRV_HTTP1=false disables HTTP/1.1 (default: on)
# WEBSRV_HTTP2=false disables HTTP/2 (default: on)
# WEBSRV_HTTP3=false disables HTTP/3 (default: on, TLS only)
WEBSRV_HTTP1=true
WEBSRV_HTTP2=true
WEBSRV_HTTP3=true

# TLS Certificates (required when HTTP_USE_SSL=true)
TLS_CERT_PATH=./src/certs/cert.pem
TLS_KEY_PATH=./src/certs/key.pem
TLS_CA_PATH=./src/certs/cert.ca-bundle

ASSET_SERVER_AUTH_KEY="your-unique-auth-key-here"